# Local bridge results

This directory receives witnessed result artifacts produced by the trusted local LM Studio bridge.

A result file is data only. It is not executable.

Do not place scripts, workflow files, credentials, or secrets here.
