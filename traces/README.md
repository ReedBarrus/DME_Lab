# Traces

This directory is reserved for captured runtime traces and reconstruction evidence.

Traces are evidence inputs, not interpretations. Any missing or unavailable trace data should remain visible as missing.

