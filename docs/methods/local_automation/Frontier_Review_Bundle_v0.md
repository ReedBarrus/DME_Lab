# DME_Lab — Frontier Review Bundle v0

**Status:** projected development handoff format
**Purpose:** minimize frontier-model context consumption while preserving enough evidence for bounded review
**Authority:** NONE

---

## Review Target

```text
task_family:
tier:
realization:
starting_commit:
candidate_id:
```

## Why Frontier Review Was Triggered

Exactly one or more:

```text
semantic ambiguity
authority boundary
unexpected scope expansion
mechanical failure after attempt budget
escalation
qualification contradiction
realization drift
Q4 promotion falsifier
new failure class
human-requested checkpoint
```

## Frozen Task Contract

```text
objective:
allowed files:
protected files:
allowed tools:
attempt budget:
mechanical success condition:
semantic invariants:
STOP condition:
```

## What Happened

```text
attempts:
terminal state:
files changed:
commands run:
mechanical result:
unexpected effects:
```

## Candidate Consequence

```text
patch / artifact:
```

## Local Evaluation

```text
mechanical_success:
promotion_error:
scope_violation:
authority_violation:
semantic_violation:
STOP_compliance:
escalation_quality:
repair_relevance:
```

## Exact Unresolved Question

State one question only.

```text
What distinction requires frontier review?
```

Do not send the frontier model the entire run merely because it exists.

## Evidence Pointers

```text
run trace:
candidate patch:
test output:
starting source:
task contract:
relevant authoritative contract:
```

## Requested Frontier Action

Exactly one:

```text
ADJUDICATE
REVISE TASK FAMILY
REVISE SEMANTIC BOUNDARY
AUTHORIZE NARROWER CONTINUATION
REJECT CANDIDATE
SELECT NEW PRESSURE
CHECKPOINT
```

## Forbidden Inference

The bundle itself does not imply:

```text
candidate is correct
local evaluator is correct
qualification survives
task family is valid
authority should expand
```

The frontier participant reviews the smallest unresolved consequential distinction.

Everything else remains local evidence.
