# Independent Authority Reconstruction v0

## Status

**PROTOCOL / ROUTING EVIDENCE**

This record preserves two independent observer reconstructions supplied after
the non-authoritative authority-routing dry run.

It does not establish scientific standing, promote a constitutional rule, or
authorize live root mutation.

Basis:

`5d720e6b4e50b94b47544cd586b063d83dce6ab0`
— `Record authority routing reconciliation dry run`.

## Input supplied to both observers

Each observer received only:

- Candidate Repair A — startup/navigation order separated from evidential
  precedence;
- Candidate Repair B — committed evidence and adjudicated decisions separated
  from derived current-state/navigation projections;
- adversarial Cases A-D.

They did **not** receive the Commander reconstructions or expected answers.

## Shared reconstruction result

Both observers correctly recovered the evidence / standing / projection boundary
in Case A:

```text
direct evidence may defeat a contradictory projection
!=
direct evidence automatically becoming new adjudicated standing
```

Both also preserved the need for adjudication before changed current standing.

Both observers preserved basis-relative historical lineage in Case D and refused
to let new evidence silently erase prior adjudication.

However, both independently returned:

```text
ROUTING INSUFFICIENT FOR THESE CASES
```

## Shared failure

The supplied candidate repairs defined evidential precedence but omitted the
transition semantics governing interpretation, pressure activation, execution
authorization, and supersession.

Observer A compressed the missing distinction as:

```text
EVALUATION / REACHABILITY
!=
EXECUTION AUTHORIZATION
```

Observer B compressed it as:

```text
evidential precedence
!=
standing transition
!=
execution authorization
```

The two responses therefore converge on the same wound.

## Case-specific result

### Case A — projection contradicted by evidence

**Recovered.**

Both observers concluded:

- direct retained evidence warrants the contradictory evidential result;
- the projection cannot overrule that evidence;
- evidence does not self-adjudicate;
- changed standing requires adjudication.

Minor residue: the supplied repair did not fully specify which projection
surface must update afterward.

### Case B — Council recommendation without Executive authorization

**Not recoverable from supplied repair alone.**

Both observers correctly refused to infer that Council agreement grants
execution authority, but also correctly noted that the supplied repair itself
did not define:

- who authorizes execution;
- what transition converts recommendation into executable authority.

### Case C — reachable pressure without activation

**Not recoverable from supplied repair alone.**

Both observers correctly treated the pressure map as a derived navigation
projection, but the supplied repair did not define the relationship among:

```text
reachable
active
authorized for execution
```

### Case D — historical adjudication versus conflicting new evidence

**Partially recovered.**

Both preserved:

- prior decision and basis;
- new evidence and basis;
- need for adjudication;
- prohibition on silent replacement.

One observer correctly noted that the supplied repair did not by itself define
historical supersession semantics strongly enough to determine whether the old
decision must remain unchanged.

## Repository comparison

The missing transition semantics already exist elsewhere in current repository
method surfaces.

`WORKFLOW.md` states that:

- Reed selects or authorizes active pressure;
- a pressure-map node becomes ACTIVE only after explicit selection;
- after adjudication, evidence and residue are recorded and maps updated;
- Twin agreement does not promote standing or activate a new pressure;
- available next action != authorized next pressure.

`docs/methods/TWINNING_PROTOCOL_v0.md` explicitly states:

```text
semantic interpretation != execution authorization
execution authorization != semantic promotion authorization
represented packet != authorized packet
```

and assigns Executive authorization before Workshop execution.

Therefore the blind-test failure does **not** currently support inventing a new
authority layer.

It instead demonstrates that the candidate routing repair was not
self-sufficient because it omitted the root method surface that already owns the
transition semantics.

## Forced distinction

The strongest result is:

```text
evidential routing
!=
authority-transition semantics
```

and, at the candidate root level:

```text
AGENT_CONTEXT
routes entry and precedence

WORKFLOW
owns adjudication / activation / authorization transitions
```

This distinction is currently pressure evidence, not a new registry entry.

## Consequence for four-root compression

The failed blind test sharpens rather than blocks the four-root hypothesis.

A compressed authority spine must not force `AGENT_CONTEXT.md` to carry the
entire authority topology.

The candidate responsibility split is now:

```text
README
what the Lab is

PROJECT_STATE
where the Lab currently stands

WORKFLOW
how evidence, standing, pressure, authorization, and execution move

AGENT_CONTEXT
how an entrant reconstructs the other three and finds deeper evidence
```

The next compression specimen should be tested as a **four-root set**, because
the blind test demonstrates that testing only routing language while withholding
the transition-owning Workflow creates a real information loss.

## Standing

```text
INDEPENDENT ROUTING RECONSTRUCTION:
FAILED AS SUPPLIED

FAILURE AGREEMENT:
STRONG

NEW AUTHORITY ONTOLOGY REQUIRED:
NO EVIDENCE

EXISTING WORKFLOW TRANSITION SEMANTICS:
SUFFICIENT CANDIDATE SOURCE FOR NEXT PRESSURE

FOUR-ROOT COMPRESSION:
REACHABLE AS NON-AUTHORITATIVE CANDIDATE

LIVE ROOT MUTATION:
NOT AUTHORIZED BY THIS RECORD
```
