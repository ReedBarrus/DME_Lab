# Memory Matrix Pressure 001 — Astra Reconstruction

**Status:** ACTIVE PRESSURE  
**Pressure ID:** MM-001  
**Specimen:** `docs/Evidence_Arc/Astra_Reconstruction/`  
**Candidate compiled packet:** `docs/candidates/memory_matrix_v0/ASTRA_RECONSTRUCTION_PACKET_001.md`

## Question

Can one high-entropy historical bundle be compiled into a substantially smaller
semantic memory packet while preserving every consequential distinction needed
to reconstruct:

- source role;
- source basis;
- authority / standing limits;
- dependency relations;
- historical position;
- unresolved residue;
- future-pressure implications;

without silently promoting the packet into evidence or current authority?

## Why this specimen

The Astra reconstruction bundle is intentionally messy in useful ways:

- it spans interpretation, campaign projection, developmental constraints,
  semantic proposal, and evidence manifests;
- several artifacts describe an older branch / continuity horizon;
- the reconstruction is explicitly not adjudicated success;
- the bundle contains claims that require checking against source owners;
- the first round-trip exposed a versioned-source wound: the imported seed copy
  ends at Article XX while immutable historical root versions retain XXI and,
  later, XXII;
- the Dynamic Semantic Basis proposal already contains a lineage-compression
  pressure and a candidate dynamic-memory model.

This makes it a strong first pressure for memory compilation.

## Local sufficiency

The smallest candidate mechanism is one human-readable compiled packet with:

- immutable source coordinates;
- declared role / standing for each source;
- consequential claims and distinctions;
- explicit relations among sources;
- unresolved/conflicting residue;
- nonclaims and source-retention boundary.

No graph database, schema registry, embedding store, vector index, or generalized
memory runtime is authorized by MM-001.

## Compositional compatibility

The packet must coexist with already-earned repository roles:

```text
continuity
!= compiled semantic memory

compiled semantic memory
!= evidence

compiled semantic memory
!= adjudicated standing

compiled semantic memory
!= current projection

compiled semantic memory
!= live authority
```

The packet may reference those surfaces; it may not replace them.

## Round-trip test

Give an independent observer **only** the candidate compiled packet.

Do not provide:

- the six Astra source artifacts;
- current conversation;
- prior observer conclusions;
- hidden repository context.

Ask the observer to reconstruct the questions in
`docs/candidates/memory_matrix_v0/ROUND_TRIP_PROMPT_001.md`.

Then compare the reconstruction against the six retained source artifacts.

## Pass criterion

PASS requires:

```text
substantially smaller hot memory surface
+
same consequential source-role distinctions
+
same basis / provenance boundaries
+
same unresolvedness
+
same authority / standing limits
+
recoverable dependency relations
```

A successful pass does **not** justify source deletion.

## Fail conditions

FAIL if the packet causes any consequential:

- source role collapse;
- path/version/basis detachment;
- interpretation → standing promotion;
- historical state → present state promotion;
- proposal → architecture/schema promotion;
- unresolved conflict → apparent resolution;
- dependency loss;
- false currentness;
- loss of a required nonclaim;
- reliance on prior conversation to restore meaning.

## Adjudication rule

If the observer finds a real loss:

```text
retain source
→ isolate smallest lost relation/distinction
→ repair packet minimally
→ rerun same immutable packet basis
```

Do not add a generalized memory schema merely because one packet failed.

## Compression / retention rule

Before successful round-trip:

```text
raw source = HOT / REQUIRED
packet = CANDIDATE
```

After successful round-trip, only this becomes reachable for evaluation:

```text
raw source = COLD-RETENTION CANDIDATE
packet = HOT-MEMORY CANDIDATE
```

Deletion remains separately unauthorized.

## Current standing

MM-001 is active.

Round 1 used a local Qwen 3 Coder observer and **FAILED**. The smallest
consequential loss was historical constitutional source closure: Packet 001
routed to a later imported seed copy ending at Article XX rather than following
the reconstruction's immutable historical source basis, where XXI existed and
a later continuity-freeze basis also retained XXII.

The candidate packet and round-trip prompt are being minimally repaired around
this earned distinction:

```text
source role/path
!=
source version

route to retained source
!=
historical basis closure
```

No memory method, packet schema, graph ontology, automatic capture pipeline, or
source deletion is yet earned.
