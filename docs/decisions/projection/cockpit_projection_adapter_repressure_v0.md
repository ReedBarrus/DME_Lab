# Cockpit Projection Adapter Re-pressure v0

## Scope and repository gate

This Stage 4C pass independently asked whether the Stage 4B P8 and P11
remediations conserve their wounds under named nearby perturbations without
semantic strengthening or healthy-source regression.

After `git fetch origin main`, local `HEAD` and `origin/main` were identical at
`6f65398e9e1b01952126e2a2456974356231df75` with a clean worktree. This exact
committed state remained the authoritative repository-smoke basis.

This was pressure only. The adapter and projection contract were frozen. A
failed semantic scenario would have been retained and classified rather than
repaired.

## Predeclared classification

- `SURVIVES`: the wound remains visible and no stronger clean interpretation
  appears.
- `CONTRACT_VIOLATION`: a plausible clean projection materially hides,
  collapses, invents, or strengthens the source condition.
- `REGRESSION`: a healthy supported shape degrades, misparses, or gains an
  unjustified diagnostic because of Stage 4B.
- `CONTRACT_AMBIGUITY`: the contract does not specify enough to adjudicate.
- `BASIS_INSUFFICIENT`: the fixture does not discriminate the question.

Execution success remained distinct from adapter survival.

## Pressure basis

The runner created isolated temporary Git repositories, committed each named
fixture, projected only the exact committed tree, and retained compact normalized
observations and diagnostics. It used no current evidence mutation, random fuzz,
generalized Markdown framework, identity system, or graph resolver.

Durable artifacts:

- runner: `src/runtime/cockpit_projection_adapter_repressure.py`;
- tests: `tests/cockpit/test_projection_adapter_repressure.py`;
- compact trace: `traces/cockpit_projection_adapter_repressure_v0.json`;
- this decision.

## Scenario adjudication

| Scenario | Named pressure | Classification | Bounded observation |
| --- | --- | --- | --- |
| R1 | P8 original wound replay | `SURVIVES` | Current standing survived; history stayed empty; `unsupported_structure` retained both raw R lines and coordinates; projection was partial. |
| R2 | P8 nearby heading variant | `SURVIVES` | `Earlier outcomes` behaved structurally like the original wound without becoming accepted history. |
| R3 | Healthy exact history | `SURVIVES` | R0/R1 parsed normally; projection remained complete; no unsupported diagnostic appeared. |
| R4 | Loose history-like prose | `SURVIVES` | “R0 was discussed previously” remained ordinary prose and triggered no history diagnostic. |
| R5 | P11 original duplicate replay | `SURVIVES` | Both differently standing PR-001 occurrences survived with distinct lines, one duplicate diagnostic, and no canonical node. |
| R6 | Identical duplicate headings | `SURVIVES` | Identical heading text remained distinguishable through committed source lines and separate bodies. |
| R7 | Duplicate relation target | `SURVIVES` | Exactly one explicit PR-003-to-PR-001 relation survived with `ambiguous`, candidate count two, and no invented edge. |
| R8 | Duplicate without relation | `SURVIVES` | Duplicate identity was diagnosed despite no explicit graph relation. |
| R9 | Unique identity control | `SURVIVES` | One explicit unique-target relation remained clean; no duplicate diagnostic or ambiguity metadata appeared. |
| R10 | Combined P8/P11 wound | `SURVIVES` | `duplicate_pressure_id` and `unsupported_structure` both survived; neither suppressed the other or selected the cleaner occurrence. |
| R11 | Current repository smoke | `SURVIVES` | The exact authoritative repository projection remained complete, current, and diagnostic-free. |
| R12 | Three-way duplicate | `SURVIVES` | All three occurrences survived; one duplicate condition remained; the explicit target reported three candidates without a winner. |

Classification totals:

| Classification | Count |
| --- | ---: |
| `SURVIVES` | 12 |
| `CONTRACT_VIOLATION` | 0 |
| `REGRESSION` | 0 |
| `CONTRACT_AMBIGUITY` | 0 |
| `BASIS_INSUFFICIENT` | 0 |

## Current repository smoke

The adapter projected exact commit
`6f65398e9e1b01952126e2a2456974356231df75` and compared it with
`origin/main`. Freshness was `current`; projection status was `complete`; no
diagnostics appeared.

Active pressure remained explicit none. PR-018 remained `OPEN` and inactive.
PR-019 remained current `BOUNDED_RESOLUTION`, with R0
`BASIS_INSUFFICIENT` and R1 `BOUNDED_RESOLUTION`. All 46 current constraints
were represented. Controller remained a `PARKED` projection document. No
duplicate diagnostic, unsupported valid-history diagnostic, relation ambiguity,
or parse failure appeared.

This specimen contained 19 pressure nodes, 24 explicit relations, 214 evidence
references, and three projection documents. These are current specimen
observations, not universal invariants.

## Verification

- existing adapter, Stage 3, Stage 4B, and Stage 4C focused tests: 39/39 passed;
- full hardware-free suite: 547/547 passed;
- runner completed and all scenarios were classified;
- compact trace reproduced the runner result exactly;
- `git diff --check`: passed.

## Bounded adjudication

The strongest supported result is:

```text
The remediated adapter survived the named original and nearby P8/P11
re-pressure specimens without silent loss, winner selection, or
healthy-source regression.
```

This does not establish general projection safety, general Markdown robustness,
arbitrary malformed-input safety, generalized graph correctness, or generalized
identity correctness.

Adapter v0 is ready for a first bounded, deliberately plain, read-only observer
UI specimen over the normalized model. This licenses pressure on usability and
visual projection only. It does not mean production ready, generally reliable,
authoritative, or safe for Controller use.

## Repository standing and stop boundary

This pass changed no adapter, projection contract, Pressure / Resolution Map,
Stage 3 or Stage 4B artifact, constraint registry, scientific evidence,
acoustic artifact, canonical ledger, Controller code or standing, website code,
chart, `D-*` constraint, or PR node.

Stage 4C stops here. UI construction does not begin inside this pass.
