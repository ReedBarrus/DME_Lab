# CURRENT_HORIZON_DISTINCTION_DEPENDENCE_001 — G6 Horizon Selection V0

STATUS:
JUSTIFIED_CANDIDATE

PREDECESSOR_STANDING:
METHOD_DISTINCTION_LOAD_ABLATION_V0_MATCHED

PREDECESSOR_RESULT:
docs/campaigns/method_distinction_load_001/pressure_runs/
METHOD_DISTINCTION_LOAD_ABLATION_V0_PRESSURE_RESULT_001.md

SPECIMEN_DISTINCTION:
WORLD_CHANGE != METHOD_CHANGE

## Horizon reinspection basis

The preceding G5 cell established only:

```
THE DISTINCTION CARRIES
SEMANTIC / REPRESENTATIONAL LOAD
IN THE EXACT G4 FRAME
```

while preserving:

```
CURRENT_LOAD_BEARING_STATUS = UNRESOLVED
```

Existing Atlas planning material already requires:

```
TARGET
+
CURRENT QUALIFIED FRAME
+
PROJECTED CONSEQUENTIAL FRAME(S)
+
KNOWN CONSERVED DISTINCTIONS / NON-COLLAPSES
```

for bounded relational-horizon planning, and identifies as a future pressure target:

```
WHICH DEPENDENCIES ARE BECOMING LOAD-BEARING?
```

Existing Atlas metabolism material further preserves:

```
DEPENDENCE
→
LOAD
→
...
→
DISTINCTION
```

and defines a distinction as a local resolution operator that makes structure
legible and operable to an actor.

## Selected current horizon

HORIZON_ID:
COGNITIVE_METHOD_APPLICABILITY_HORIZON_V0

DECLARED PURPOSE:

Determine whether a method-specific next pressure is applicable after an
observed post-consequence change, while refusing to infer method change from
world change or generic change alone.

The horizon asks only:

```
IS METHOD-SPECIFIC CONTINUATION
CURRENTLY APPLICABLE?
```

It does not decide whether the method is good, should be retained, or should be
capitalized.

## Exact applicability law

Using the exact G4 axes:

```
METHOD_ONLY
→ METHOD_SPECIFIC_PRESSURE_APPLICABLE

BOTH
→ METHOD_SPECIFIC_PRESSURE_APPLICABLE

WORLD_ONLY
→ METHOD_SPECIFIC_PRESSURE_NOT_APPLICABLE

NEITHER
→ METHOD_SPECIFIC_PRESSURE_NOT_APPLICABLE

METHOD_UNRESOLVED
→ METHOD_SPECIFIC_PRESSURE_UNRESOLVED

WORLD_UNRESOLVED
→ METHOD_SPECIFIC_PRESSURE_NOT_APPLICABLE
```

WORLD_UNRESOLVED remains non-applicable because the method axis is explicitly
UNCHANGED in the frozen G4 specimen.

## Ablated condition

If WORLD_CHANGE != METHOD_CHANGE is removed and only:

```
GENERIC_CHANGE_POSTURE
```

remains, then:

```
CHANGED
```

cannot distinguish:

```
WORLD_ONLY
METHOD_ONLY
BOTH
```

and therefore cannot deterministically decide whether method-specific
continuation is applicable.

Likewise generic:

```
UNRESOLVED
```

cannot distinguish:

```
WORLD_UNRESOLVED
METHOD_UNRESOLVED
```

and therefore cannot preserve the same applicability posture.

## Selected G6 gap

GAP_ID:
G6_CURRENT_HORIZON_DISTINCTION_DEPENDENCE

QUESTION:

Does preserving WORLD_CHANGE != METHOD_CHANGE carry current semantic /
coordination load for the exact declared
COGNITIVE_METHOD_APPLICABILITY_HORIZON_V0 because its removal makes the
horizon's applicability decision underdetermined?

## Claim ceiling

This horizon selection justifies one bounded dependence pressure only.

It does not establish:

- method improvement;
- retention justification;
- method capitalization;
- causal benefit;
- automatic planning;
- autonomous gap discovery;
- policy mutation;
- authority;
- execution;
- scientific standing.
