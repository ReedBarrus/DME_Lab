# LIVE_COMPLETION_ADAPTER_QUALIFICATION_001 — Pressure Design

The sole target is the semantic membrane between the repaired v1 live-completion
evidence bridge and the exact authoritative lifecycle controller.

```text
RAW PINNED BASIS
→ RECONSTRUCT
→ DERIVE P05/P06
→ REPRODUCE QUALIFIED P07/P08
→ PROJECT
→ DIGEST / VERIFY
→ IMMEDIATELY INVOKE EXACT CONTROLLER
→ WRAP / RELABEL RESULT
```

No caller supplies P05/P06/P07/P08, blocker booleans, bounded-unit identity,
receipt outcome, controller input, controller claim projection, or admissibility.

Cells A–L implement the warrant exactly. In particular:

- B/C/D/G/H/I must stop before controller invocation;
- E demonstrates no public outcome-relabeling surface;
- F demonstrates candidate producer bytes do not equal runtime qualification;
- J mutates the internally derived projection and requires the digest guard to
  reject before controller invocation;
- K preserves source historical claim != controller claim projection;
- L requires COMPLETE admissible with zero transition/lane/claim/occupant mutation.

PASS qualifies only this exact adapter contract, implementation, frozen Lane-A
specimen, repaired v1 bridge, and exact lifecycle-controller blob.
