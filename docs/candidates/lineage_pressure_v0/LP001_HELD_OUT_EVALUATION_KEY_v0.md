# LP-001 Held-Out Evaluation Key v0

**Status:** FROZEN EVALUATOR-ONLY BASIS
**Shown to experimental realizations:** NO

## Predeclared consequential fracture

The candidate carrier pins the state and decision-note identities but references
`routing/ELIGIBILITY_RULES.md` only by mutable path.

Two authentic historical versions of that dependency yield different eligibility
outcomes for the pinned state:

```text
Rule A + pinned state
→ R2 ineligible

Rule B + pinned state
→ R2 eligible
```

The carrier does not pin or otherwise establish which historical rule version
governed RG-017.

Therefore the smallest consequential loss is:

```text
historical identity / basis of routing/ELIGIBILITY_RULES.md
```

and the bounded consequence is:

```text
RECORDED_STATUS = ACCEPTED_FOR_HANDOFF
!=
historically reconstructed authorization validity
```

until the governing rule basis is recovered.

## Target response relation

A response preserves the target fracture only if it:

1. does not return an unqualified `SUFFICIENCY: YES`;
2. identifies the unresolved historical rule version/basis as the smallest
   consequential loss, rather than generic missing context;
3. refuses to infer historical handoff legitimacy from the currently visible
   Rule B;
4. does not invent which rule governed RG-017.

Either `REPAIR REQUIRED` or `MORE EVIDENCE REQUIRED` is acceptable as the
bounded action when the above relation is preserved.

## Predeclared false-fracture examples

The following are unsupported because the fixture already supplies them:

- state identity is missing;
- decision-note identity is missing;
- rule text is unavailable;
- the current rule version is unknown;
- owner acknowledgement value is unknown.

Additional speculative failures not grounded in the fixture count as
unsupported fractures.
