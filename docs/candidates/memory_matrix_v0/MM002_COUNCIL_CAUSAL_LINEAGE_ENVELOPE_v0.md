# MM-002 Council Causal Lineage Envelope v0

**Status:** CANDIDATE MINIMAL REPAIR — ROUND 3  
**Purpose:** preserve actor/round causal attribution for consequential proposal
transitions without duplicating the raw Council rounds or rewriting the
checkpoint.

This envelope composes with:

- `traces/multi_round_deliberation_001_checkpoint.json`
- `docs/candidates/memory_matrix_v0/MM002_CHECKPOINT_BASIS_ENVELOPE_v0.md`

It adds only the causal lineage that Round 2 exposed as missing.

## Causal sequence

### CHATGPT-1 — introduce inspect candidate

Source blob:
`55dc0e28cb3e78681a7c43254d8d5ba829e9dae6`

Causal role:

```text
INTRODUCES
read-only inspect operation
(cursor + ordered unread delta + ref availability)

SELF-LIMITS
inspect convenience
!= automatic delivery
!= semantic application
!= repeated reliability
```

The round proposes inspect as the smallest implementation candidate while
already restricting what success could establish.

### CODEX-1 — attack implementation priority; preserve inspect only as comparison candidate

Source blob:
`eb326bcf32a727c474cbe53a2eb0c916e210afd5`

Causal role:

```text
ATTACKS
"implement inspect next"

BECAUSE
existing AGENT_CONTEXT + delta already provide core reads
AND
no retained evidence shows multi-read burden is the consequential wound

IDENTIFIES STRONGER PRESSURE
fresh invocation performing startup ritual without human prompt

PRESERVES
inspect envelope only as isolated utility-comparison candidate
```

This round introduces:

```text
startup invocation failure
!=
multi-read reconstruction burden
!=
reference-integrity failure
```

It does not finally adjudicate the comparison candidate; it removes immediate
inspect implementation from priority and redirects attention toward invocation
reliability.

### CHATGPT-2 — replace inspect path with startup-pointer candidate

Source blob:
`f5afc17abbfd72992fa8b9a69a124be3b51361fd`

Causal role:

```text
USES
Codex-1 invocation-boundary criticism

OBSERVES
AGENT_CONTEXT is durable documentation
BUT
no root AGENTS.md exists as a demonstrated startup-injection surface

REPLACES
inspect-command proposal

WITH
minimal root AGENTS.md pointer
→ existing continuity ritual
→ one fresh Codex invocation pressure
```

This is the round that explicitly records the read-only inspect implementation
as **displaced**.

It introduces:

```text
repository-documented protocol
!=
runtime-injected startup instruction
!=
observed startup compliance
```

The prior A/B inspect-envelope idea is therefore **superseded by the narrower
startup-boundary pressure as the selected next proposal**, but the raw evidence
does not support claiming it was separately falsified.

### CODEX-2 — accept and further bound the startup-pointer pressure

Source blob:
`37445955d56bba9893b533a70af60d6483409911`

Causal role:

```text
ACCEPTS
root AGENTS.md pointer
+
one fresh Codex invocation pressure

NARROWS SCOPE TO
Codex startup discoverability

ATTACKS OVERCLAIMS
installed file
!= delivery
!= compliance
!= semantic application
!= roundtrip reliability

RECOMMENDS STOP
because one repository-local,
reversible,
measurable proposal
is now smaller than displaced alternatives
```

This round introduces:

```text
instruction installed
!=
instruction injected
!=
ritual performed
!=
event semantically applied
```

## Consequential transition graph

```text
CHATGPT-1
INTRODUCES inspect implementation candidate
        ↓
CODEX-1
ATTACKS immediate implementation
PRESERVES inspect only as comparison candidate
IDENTIFIES startup invocation as stronger unresolved wound
        ↓
CHATGPT-2
USES invocation-boundary criticism
REPLACES inspect path with root-pointer + fresh-invocation pressure
EXPLICITLY DISPLACES inspect implementation
        ↓
CODEX-2
ACCEPTS root-pointer pressure
NARROWS it to Codex startup discoverability
RECOMMENDS STOP
        ↓
CHECKPOINT
records surviving proposal,
fractured alternatives,
earned distinctions,
unresolved residue,
and no authority/standing change
```

## Important ambiguity preserved

The A/B inspect-envelope comparison proposed by Codex-1 is not separately
recorded as falsified.

The warranted relation is:

```text
preserved as comparison candidate at CODEX-1
→ later superseded / not selected as next pressure
```

not:

```text
experimentally disproved
```

## Repair boundary

This envelope adds:

- actor identity;
- round identity;
- proposal-transition relation;
- causal reason for attack / replacement / acceptance;
- explicit preservation of the A/B candidate's non-falsified status.

It does **not** duplicate:

- full round prose;
- every attack qualification;
- every architecture-not-earned list;
- exact wording already recoverable from immutable blobs;
- new authority or standing.

If Round 3 still requires additional detail for correct reconstruction, that
detail must be exposed as a new independent loss.
