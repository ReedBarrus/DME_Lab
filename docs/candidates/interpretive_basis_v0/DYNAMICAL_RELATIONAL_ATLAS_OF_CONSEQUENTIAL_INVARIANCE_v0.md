# DYNAMICAL_RELATIONAL_ATLAS_OF_CONSEQUENTIAL_INVARIANCE_v0

```text
OBJECT_TYPE:
CANDIDATE_PRIMARY_INTERPRETIVE_BASIS

OBJECT_ID:
DYNAMICAL_RELATIONAL_ATLAS_OF_CONSEQUENTIAL_INVARIANCE_v0

SHORT_NAME:
DRACI_v0

STATUS:
PROVISIONAL
MOVING
PRESSURE-EXPOSED
NON-CANONICAL

STANDING:
CANDIDATE PRIMARY BASIS FOR THE NEXT DEVELOPMENT LEG

AUTHORITY_EFFECT:
NONE

EXECUTION_EFFECT:
NONE

ONTOLOGY_FREEZE:
NO

ARCHITECTURE_FREEZE:
NO

SCHEMA_FREEZE:
NO
```

## 0. PURPOSE

Provide one provisional mathematical / geometric legend through which
current DME mechanisms, debts, actions, recoveries, identities,
currentness, authority, pressure, correction, influence, and consequence
may be jointly interpreted and compared.

The Atlas exists to improve:

- navigation;
- local reconstruction;
- pressure selection;
- debt interpretation;
- causal pathing;
- recoverability analysis;
- action-surface legibility;
- development leverage.

It does **not** establish that reality is literally described by this model.

It does **not** supersede qualified Lab mechanisms.

It does **not** grant standing to a mechanism merely because that mechanism
can be represented by Atlas coordinates.

### Primary epistemic law

```text
IF:

LAB EVIDENCE
!=
ATLAS PROJECTION

THEN:

ATLAS MUST MOVE
UNLESS FRESH PRESSURE FRACTURES THE LAB BASIS.
```

## 1. DEVELOPMENT POSTURE

The Atlas is:

```text
A LEGEND
A NAVIGATION SURFACE
A PROJECTION
A COMPARISON FRAME
A FALSIFIABLE COORDINATE SYSTEM
```

The Atlas is not:

```text
A UNIVERSAL ONTOLOGY
A UTILITY FUNCTION
A GOVERNANCE SYSTEM
A PLANNER
AN AUTHORITY SOURCE
A PROOF OF STANDING
A REPLACEMENT FOR EXPERIMENT
```

Plans derived from the Atlas are:

```text
SUGGESTIVE PATHING ONLY.

PLAN
!=
AUTHORITY

PATH EFFICIENCY
!=
ADMISSIBILITY

PREDICTED LEVERAGE
!=
EXECUTION WARRANT

MAP LEGIBILITY
!=
REALITY
```

## 2. PRIMARY BASIS — CONSEQUENCE

The Atlas begins with consequence rather than with role, authority,
agent, seat, influence, correction, or planning.

Primitive situated relation:

```text
R = (s, o, b, t)

s = subject configuration
o = object / referent configuration
b = operative basis
t = observation / execution coordinate
```

Associated relational state:

```text
x_R(t)
```

Primary mapping:

```text
RELATION
→
STATE
→
CONSEQUENCE
```

Let consequence be:

```text
C_R : X_R → 𝓒

C_R(t) = C_R(x_R(t))
```

`𝓒` is not assumed scalar.

Possible consequence dimensions may include:

```text
semantic
operational
authority
provenance
physical
social
financial
computational
historical
etc.
```

These dimensions are **not** frozen ontology.

No universal goodness scalar is assumed.

## 3. ACTION AS TRANSFORMATION

An action is provisionally represented as:

```text
A_R : x → x'
```

Observed consequential displacement:

```text
Δ_A C_R
=
C_R(A_R(x)) - C_R(x)
```

Full observed action change may be represented as:

```text
Δ_R A
=
(
ΔC_R,
Δθ_s,
Δθ_o,
Δb,
Δt,
...
)
```

No multiplicative interaction law is currently assumed.

The Atlas therefore treats these first as coordinates of transformation,
not as already-known coupled quantities.

## 4. LOCAL CONFIGURATION

Introduce provisional local configuration coordinates:

```text
θ_s = subject-side configuration / coherence coordinate

θ_o = object-side configuration / coherence coordinate
```

`θ` does not yet mean literal angle.

Locally:

```text
x_R = x_R(θ_s, θ_o, b, t, ...)

C_R = C_R(θ_s, θ_o, b, t, ...)
```

Local consequential change may therefore be decomposed as:

```text
dC_R
=
(∂C_R / ∂θ_s)dθ_s
+
(∂C_R / ∂θ_o)dθ_o
+
(∂C_R / ∂b)db
+
(∂C_R / ∂t)dt
+
...
```

Interpretive value:

```text
CONSEQUENTIAL CHANGE
CAN BE EXAMINED IN TERMS OF
WHICH RELATIONAL COORDINATE MOVED.
```

## 5. SENSITIVITY

Define provisionally:

```text
S_s(R)
=
∂C_R / ∂θ_s

SUBJECT-SIDE CONSEQUENCE SENSITIVITY
```

and:

```text
S_o(R)
=
∂C_R / ∂θ_o

OBJECT-SIDE CONSEQUENCE SENSITIVITY
```

Important repair:

```text
DO NOT FREEZE:

S_s = INFLUENCE

S_o = CORRECTION
```

Instead, **influence** and **correction** are relational roles of admissible
transformations acting through the geometry.

Provisionally:

```text
INFLUENCE
=
an admissible transformation producing consequential displacement

CORRECTION
=
an admissible transformation reducing relevant deviation,
restoring declared invariants,
containing consequence,
or repositioning the relation toward a recoverable regime
```

The same transformation may function as influence in one relation and
correction in another.

## 6. OPERATOR POSSIBILITY, REACHABILITY, AND ADMISSIBILITY

Let:

```text
𝓤(x)
=
operators / actions that exist from or around state x
```

Distinguish three geometries:

```text
POSSIBILITY GEOMETRY:
what transformations exist in principle

REACHABILITY GEOMETRY:
what transformations can actually be reached from the current state

ADMISSIBILITY GEOMETRY:
what transformations have sufficient local basis to be exercised
```

Freeze the distinction:

```text
MECHANISM EXISTS
!=
MECHANISM REACHABLE
!=
MECHANISM ADMISSIBLE
```

Let an admissibility predicate be provisionally:

```text
α(A, x, b) ∈ {0,1}
```

or later some richer standing space if binary admission proves inadequate.

Then:

```text
𝓤_A^adm(x,b)
=
{ A ∈ 𝓤(x) : α(A,x,b)=1 }
```

Authority therefore is not an identity property.

It provisionally constrains which operators are locally admissible.

## 7. LOCAL AUTHORITY

Authority is provisionally interpreted as relationally emergent.

Candidate authority coordinate:

```text
AUTHORITY
=
f(
operator,
subject,
action,
basis,
consequence range,
corrective path
)
```

Therefore:

```text
HUMAN
!=
INHERENT AUTHORITY

AI
!=
INHERENT NON-AUTHORITY

ROLE
!=
AUTHORITY

PLAN
!=
AUTHORITY

CAPABILITY
!=
AUTHORITY

COORDINATION CLEAR
!=
AUTHORITY
```

Authority may vary across:

- subject;
- action type;
- consequence surface;
- basis;
- currentness;
- recoverability;
- corrective support;
- relational dependency.

Current human stewardship is therefore:

```text
HIGH-CONSEQUENCE,
HIGH-CONTEXT,
EXTERNAL-WORLD RELATIONAL STANDING

NOT:

MYTHICAL
ABSOLUTE
UNREVIEWABLE
INFORMATIONALLY DESPOTIC
PERMANENT BY IDENTITY
```

## 8. CONSEQUENTIAL REACH

Let:

```text
𝓡_A(x)
=
{
C(A(x)) - C(x)
|
A ∈ 𝓤_A^adm(x)
}
```

This is the currently admissible consequential region reachable through
the local action surface.

Important:

```text
ACTION RANGE
!=
ACTION COUNT
```

A small number of actions may span a large consequence region.

A large number of actions may remain locally inconsequential.

## 9. INVARIANTS AND IDENTITY

Let:

```text
𝓘 = {I₁, I₂, ..., Iₙ}
```

be the declared invariants relevant to the local transformation.

These may include:

```text
identity
lineage
provenance
authority boundary
claim correspondence
historical conservation
subject continuity
recoverability constraints
etc.
```

Define equivalence under the declared invariant set:

```text
x ~_𝓘 y
```

iff relevant invariants remain equivalent within declared tolerances.

Therefore:

```text
x' != x
```

can coexist with:

```text
x' ~_𝓘 x
```

Freeze:

```text
TRANSFORMATION
!=
LOSS OF RELEVANT IDENTITY
```

Identity in this Atlas is therefore not literal state equality.

It is recoverable / conserved equivalence under a declared scope.

## 10. CORRECTIVE PATHS AND RECOVERABILITY

Suppose:

```text
x' = A(x)
```

`A` is recoverable under invariant set `𝓘` only where there exists an
admissible reachable corrective path `K` such that:

```text
K(A(x)) ~_𝓘 x
```

within declared bounds on:

```text
time
cost
error
consequence
risk
distance
or other relevant dimensions
```

Thus:

```text
RECOVERABILITY
=
CORRECTIVE POSSIBILITY
+
CORRECTIVE REACHABILITY
+
INVARIANT RESTORATION
```

Not:

```text
"AN UNDO FUNCTION EXISTS"
```

A corrective mechanism that cannot be reached from the failure state
does not establish recoverability.

## 11. CURRENTNESS

The operative basis is dynamic:

```text
b = b(t)
```

Therefore:

```text
R(t)
=
(s(t), o(t), b(t), t)
```

Even if nominal identities remain unchanged:

```text
R(t₀)
may not be equivalent to
R(t₁)
```

because:

```text
b(t₀) != b(t₁)
```

and therefore available / reachable / admissible operators may differ.

Currentness provisionally means:

```text
THE TIME-LOCAL CONDITIONING
OF WHICH RELATIONS,
OPERATORS,
BASES,
CORRECTIVE PATHS,
AND CONSEQUENCE SURFACES
ARE ACTUALLY AVAILABLE NOW.
```

Staleness is therefore not merely bad metadata.

Staleness can alter the reachable consequence geometry.

## 12. SEATS

A seat is provisionally modeled as a bounded operator regime:

```text
Σ_q(x,b)
⊆
𝓤(x,b)
```

Changing:

```text
occupant
invocation
claim
basis
authority
currentness
coordination
available tooling
environment
```

may alter:

```text
Σ_q
```

Freeze:

```text
SEAT IDENTITY
!=
OCCUPANT IDENTITY
!=
INVOCATION IDENTITY
!=
WORK CLAIM
!=
AVAILABLE OPERATOR SET
```

A seat is therefore **not** required to be modeled as a mystical persistent agent.

It may initially be treated as:

```text
A BOUNDED RELATIONAL REGIME
DETERMINING WHICH TRANSFORMATIONS
ARE AVAILABLE FROM AN OPERATING POSITION.
```

## 13. PRIMARY STABILITY HYPOTHESIS

For recurring action regime `A`, let:

```text
𝔠_A(x)
=
its admitted consequential reach
```

Let:

```text
𝔯_𝓘(x)
=
the region from which an admissible corrective path can restore
the required invariant set
```

Candidate stability condition:

```text
𝔠_A(x)
⊆
𝔯_𝓘(x)
```

Verbally:

```text
THE CONSEQUENTIAL REACH ADMITTED
TO A RECURRING ACTION REGIME
SHOULD REMAIN INSIDE THE REGION
FROM WHICH REQUIRED INVARIANTS
REMAIN RECOVERABLE.
```

This is a **pressure hypothesis**, not yet law.

## 14. UNCOVERED CONSEQUENTIAL REGION

Define provisionally:

```text
𝓓_uncorrected
=
𝔠_A(x)
𝔯_𝓘(x)
```

This is the:

```text
UNCOVERED CONSEQUENTIAL REGION
```

Interpretively:

```text
ACTION CAN REACH HERE
BUT QUALIFIED CORRECTION / RECOVERY CANNOT YET FOLLOW
```

This region is a high-value pressure target.

Candidate debt interpretations:

```text
TYPE A:
CONSEQUENCE REACH EXISTS
CORRECTIVE REACH MISSING

TYPE B:
CORRECTIVE MECHANISM EXISTS
BUT IS UNREACHABLE

TYPE C:
ACTION IS REACHABLE
BUT ADMISSIBILITY BASIS IS UNRESOLVED

TYPE D:
RECOVERY EXISTS
BUT REQUIRED INVARIANTS ARE NOT RESTORED

TYPE E:
FIELD CHANGED THROUGH STALENESS
AND PREVIOUS CORRECTIVE PATH NO LONGER APPLIES

TYPE F:
CORRECTIVE ACTION ITSELF CREATES
A NEW UNCOVERED CONSEQUENTIAL REGION
```

## 15. RECIPROCAL INFLUENCE / CORRECTION LAW

Primary questions:

```text
HOW FAR CAN INFLUENCE PROPAGATE
BEFORE A CORRECTIVE RELATION IS REQUIRED?

HOW FAR CAN CORRECTION PROPAGATE
BEFORE ITS OWN CONSEQUENCES REQUIRE
AN INFLUENTIAL / CORRECTIVE RESPONSE?
```

Therefore:

```text
CORRECTION
IS ITSELF CONSEQUENTIAL INFLUENCE
```

No corrective subsystem receives exemption from Atlas analysis.

```text
A SAFETY LAYER
CAN ITSELF REQUIRE CORRECTION.
```

## 16. RECURRING INFLUENCE

Candidate minimum constraint:

```text
NO RECURRING INFLUENCE
WITHOUT A CORRECTIVE PATH
```

Stronger candidate:

```text
ACTION CAPACITY
SHOULD NOT OUTRUN
CORRECTIVE CAPACITY
```

More precise Atlas form:

```text
RECURRING CONSEQUENTIAL REACH
SHOULD REMAIN INSIDE
RECOVERABLE REACH
```

This applies provisionally to:

```text
human attention
AI attention
planning
review
warrants
execution
correction
memory
projection
routing
seats
claims
authority decisions
environmental response
governance mechanisms
```

Nothing receives a mythical exemption because of its label or identity.

## 17. PRESSURE

Pressure is provisionally:

```text
AN EXPERIMENTAL PERTURBATION
OF A RELATIONAL GEOMETRY
```

Pressure may test:

- action reach;
- corrective reach;
- admissibility;
- basis currentness;
- identity invariance;
- reconstruction;
- recovery;
- consequence propagation;
- relation correspondence;
- threshold behavior;
- buffering;
- boundary conditions;
- residue;
- failure localization.

Preferred pressure progression:

```text
PERTURB
↓
OBSERVE CONSEQUENCE
↓
LOCATE DEVIATION
↓
IDENTIFY MISSING / FAILED CORRECTIVE PATH
↓
RECOVER BASIS
↓
REPAIR / BUFFER / REPOSITION / THRESHOLD
↓
RECONSTRUCT
↓
REPRESSURE
```

Freeze:

```text
PRESSURE PASS
!=
SYSTEM COMPLETE

PRESSURE FAILURE
!=
ARCHITECTURE REQUIRED
```

## 18. DEVELOPMENT DEBT AS GEOMETRY

Debt should be projected where possible as a local mismatch between:

- consequential reach;
- corrective reach;
- admissibility;
- currentness;
- recoverability;
- invariant conservation;
- observation;
- reconstruction.

Debt therefore becomes less like:

```text
"AN UNRESOLVED DOCUMENT"
```

and more like:

```text
"A LOCATED DEFICIENCY IN THE CURRENT RELATIONAL FIELD"
```

Examples:

```text
UNOWNED ACTION EDGE

UNCORRECTED CONSEQUENCE REGION

STALE BASIS

UNREACHABLE RECOVERY MECHANISM

UNQUALIFIED ADMISSIBILITY EDGE

BROKEN IDENTITY INVARIANCE

MISSING CORRESPONDENCE

UNOBSERVABLE CONSEQUENCE

IRREVERSIBLE ACTION WITHOUT ADEQUATE PRE-ACTION BASIS
```

## 19. LEVERAGE

Leverage is not yet formally defined.

Candidate interpretation:

```text
LEVERAGE
≈
CONSEQUENTIAL DISPLACEMENT OBTAINABLE
PER UNIT OF ADMITTED TRANSFORMATION,
CORRECTIVE BURDEN,
AND DEVELOPMENT COST
```

Do not freeze a scalar ratio yet.

High-leverage development surfaces are likely those where:

```text
small bounded intervention
→
large reduction in uncovered consequential region
```

or:

```text
small new corrective path
→
large increase in safe action reach
```

or:

```text
one integration
→
multiple previously disconnected qualified mechanisms become jointly usable
```

## 20. ATLAS OBJECT PROJECTION

For any development object, attempt to project:

```text
RELATIONAL SUBJECT

OBJECT / REFERENT

CURRENT BASIS

CURRENT STATE

AVAILABLE OPERATORS

REACHABLE OPERATORS

ADMISSIBLE OPERATORS

EXPECTED CONSEQUENCE DELTA

OBSERVED CONSEQUENCE DELTA

DECLARED INVARIANTS

IDENTITY / EQUIVALENCE BASIS

CORRECTIVE OPERATORS

CORRECTIVE REACH

RECOVERABLE REGION

UNCOVERED CONSEQUENTIAL REGION

CURRENTNESS DEPENDENCIES

OBSERVABILITY

TRAJECTORY

PRESSURE VECTOR

BUFFER / THRESHOLD

RECOVERY PATH

LEVERAGE

UNRESOLVED COORDINATES
```

If these cannot be projected without invention:

```text
ATLAS_MAPPING_UNRESOLVED
```

Do not fabricate coordinates.

## 21. ATLAS SELF-PRESSURE

The model itself must be pressured.

For every mapped mechanism ask:

1. Does the Atlas preserve the distinctions the Lab already earned?
2. Does the Atlas require fake quantities or fake continuity?
3. Does the Atlas collapse distinct relations into one axis?
4. Does the Atlas create a false scalar ordering?
5. Does it confuse possible, reachable, admissible, authorized, executed, observed, corrected, recovered?
6. Does it preserve `UNRESOLVED != NEGATIVE`?
7. Does its recoverability claim depend on a corrective path that cannot actually be reached?
8. Does the mapped consequence space omit a dimension already shown to be causally relevant?

If the model fails:

```text
REPAIR THE ATLAS

NOT THE EVIDENCE.
```

## 22. RELATION TO CURRENT FIVE-STEP DEVELOPMENT PROGRAM

`DRACI_v0` is the candidate interpretive basis for the next major development leg.

It does **not** replace:

```text
SEAT_LOOP_CAUSAL_METABOLISM_STRATEGY_001
```

Instead:

```text
SEAT_LOOP_CAUSAL_METABOLISM_STRATEGY_001
=
candidate development trajectory

DRACI_v0
=
candidate map / legend through which that trajectory
and its pressure/debt are interpreted
```

Each major step should therefore produce, where evidence permits:

```text
ATLAS ENTRY STATE

ACTION SURFACE

EXPECTED CONSEQUENCE

OBSERVED CONSEQUENCE

INVARIANTS

CORRECTIVE SURFACE

RECOVERABILITY RESULT

UNCOVERED REGION

DEBT CREATED / REMOVED

NEXT PRESSURE VECTOR
```

The five-step plan remains suggestive.

`DRACI_v0` creates no authority to execute any step.

## 23. PRIMARY DEVELOPMENT LOOP

```text
MAP EXISTING MECHANISM
↓
PROJECT INTO ATLAS
↓
LOCATE CONSEQUENTIAL / CORRECTIVE MISMATCH
↓
SELECT SMALLEST CONSEQUENTIAL PRESSURE
↓
EXECUTE ONLY UNDER EXISTING ADMISSION / AUTHORITY RULES
↓
OBSERVE REAL RESULT
↓
RECONSTRUCT TRAJECTORY
↓
COMPARE LAB RESULT AGAINST ATLAS
↓
REVISE LOSER
↓
CONSERVE SURVIVING RELATION
↓
REPEAT
```

## 24. CURRENT PRIMARY HYPOTHESES

```text
H1:
CONSEQUENCE IS A USEFUL PRIMARY INTERPRETIVE BASIS.

H2:
ACTION IS BETTER REPRESENTED AS TRANSFORMATION
THAN AS A PREMATURE MULTIPLICATIVE SCALAR.

H3:
INFLUENCE AND CORRECTION ARE RELATIONAL ROLES,
NOT FIXED SUBSTANCES OR IDENTITIES.

H4:
POSSIBILITY, REACHABILITY, AND ADMISSIBILITY
ARE NON-EQUIVALENT GEOMETRIES.

H5:
CURRENTNESS CHANGES THE LOCAL REACHABLE FIELD.

H6:
IDENTITY CAN BE REPRESENTED PROVISIONALLY
AS EQUIVALENCE UNDER DECLARED INVARIANTS.

H7:
RECOVERABILITY REQUIRES
REACHABLE INVARIANT-RESTORING CORRECTION.

H8:
RECURRING CONSEQUENTIAL REACH
SHOULD REMAIN INSIDE
RECOVERABLE REACH.

H9:
THE DIFFERENCE BETWEEN THOSE REGIONS
IS A USEFUL DEVELOPMENT PRESSURE SURFACE.

H10:
AUTHORITY IS LOCALLY RELATIONAL
AND CONSTRAINS ADMISSIBLE OPERATOR SETS.

H11:
SEATS CAN BE INTERPRETED AS
BOUNDED OPERATOR REGIMES
WITHOUT ASSUMING PERSISTENT AGENT ONTOLOGY.

H12:
A PLAN IS ONLY A PROJECTION
OF POSSIBLE EFFICIENT PATHING
THROUGH THE CURRENT FIELD.

H13:
CORRECTIVE MACHINERY IS ITSELF CONSEQUENTIAL
AND THEREFORE REQUIRES CORRECTIVE PATHING.

H14:
THE ATLAS SHOULD BE UPDATED
WHEN EVIDENCE DEMONSTRATES
THAT ITS GEOMETRY IS WRONG.
```

## 25. NON-CLAIMS

`DRACI_v0` does not claim:

- reality is fundamentally geometric;
- consequence space has these exact dimensions;
- `θ` is literally angular;
- derivatives are always numerically measurable;
- all relations are smooth;
- all actions are reversible;
- all invariants are continuous;
- authority is binary;
- recoverability is scalar;
- seats are fully described by operator sets;
- influence and correction have final definitions;
- the stability subset condition is universally sufficient;
- all DME debt can be geometrically represented;
- autonomous recurrence is currently safe;
- this candidate is correct.

## 26. PROMOTION / SURVIVAL CRITERION

`DRACI_v0` should survive only if:

1. existing qualified Lab mechanisms can be projected without losing earned distinctions;
2. the model exposes useful pressure targets that were previously difficult to locate;
3. recoverability / reachability distinctions improve development decisions;
4. the model predicts or localizes real fractures;
5. real pressure can falsify its projections;
6. it reduces conceptual duplication;
7. it improves reconstruction and pathing;
8. it does not acquire authority merely by repeated use.

Promotion is not:

```text
"EVERYONE AGREES."
```

Promotion is:

```text
THE MODEL CONTINUES TO EARN USEFULNESS
UNDER ADVERSARIAL APPLICATION.
```

## 27. IMMEDIATE USE

For the next major development steps, use `DRACI_v0` to ask:

```text
WHERE IS THE CURRENT CONSEQUENTIAL REACH?

WHERE IS THE CURRENT CORRECTIVE REACH?

WHAT INVARIANTS ARE REQUIRED?

WHAT ACTION SURFACE IS ACTUALLY ADMISSIBLE?

WHAT PART OF THE CONSEQUENCE REGION
IS NOT YET RECOVERABLE?

WHICH DEBT LIES ON THAT BOUNDARY?

WHAT IS THE SMALLEST INTERVENTION
THAT WOULD INCREASE CORRECTIVE COVERAGE
OR REDUCE UNSUPPORTED ACTION REACH?

WHAT REAL OBSERVATION WOULD
FALSIFY THE CURRENT MAP?
```

## 28. DISPOSITION

```text
DISPOSITION:

PROPOSE FOR ADVERSARIAL REVIEW
AND USE AS TEMPORARY PRIMARY INTERPRETIVE BASIS
```

Request to reviewers:

```text
DO NOT ASK WHETHER DRACI_v0
IS ELEGANT OR COMPLETE.

ATTACK:

- false equivalences;
- missing consequence dimensions;
- fake coordinates;
- broken currentness;
- unrecoverable corrective assumptions;
- hidden authority;
- lost invariants;
- wrong operator geometry;
- misleading topology;
- incorrect ordering;
- places where the Atlas predicts
  a pressure surface that the Lab does not contain.
```

Primary review question:

```text
WHERE DOES THIS MAP
FAIL TO PRESERVE
THE REAL RELATIONAL DYNAMICS
ALREADY EARNED BY THE LAB?
```

Final law:

```text
THE MAP MAY GUIDE THE EXPEDITION.

THE MAP DOES NOT OWN THE TERRITORY.
```
